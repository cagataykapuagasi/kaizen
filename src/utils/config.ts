import { initialWindowMetrics } from 'react-native-safe-area-context';

export const tabBarHeight = 68 + (initialWindowMetrics?.insets.bottom || 0);
export const baseURL = 'https://api.extrazone.com';
